# The machine-level npm config sets `omit=dev`, which would skip the toolchain
# (typescript, wrangler, tsx, vite) this repo needs to build and deploy.
# `production=false` is npm's supported way to reset that.
production=false
